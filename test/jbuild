(jbuild_version 1)

(executables
    ((libraries (irmin-redis))
     (names (test))
     (modules (test))))

(alias
    ((name runtest)
     (package irmin-redis)
     (deps (test.exe))
     (action (run ${<}))))
